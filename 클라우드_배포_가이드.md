# 클라우드 배포 가이드
## 온누리인쇄나라 서버 클라우드 배포

PC 환경이 아니더라도 서버가 24시간 구동되도록 클라우드에 배포하는 방법입니다.

---

## 📋 클라우드 플랫폼 선택

### 🆓 무료 플랫폼 (추천)

#### 1. **Railway** ⭐⭐⭐ (가장 추천)
- **장점**: 무료 티어 제공, 설정 간단, 자동 배포
- **무료**: 월 5달러 크레딧 (충분함)
- **URL**: https://railway.app

#### 2. **Render** ⭐⭐
- **장점**: 무료 티어 제공, GitHub 연동 쉬움
- **무료**: 무료 플랜 (15분 비활성 시 슬리프 모드)
- **URL**: https://render.com

#### 3. **PythonAnywhere** ⭐
- **장점**: Python 전용, 설정 간단
- **무료**: 제한적 무료 플랜
- **URL**: https://www.pythonanywhere.com

### 💰 유료 플랫폼 (더 안정적)

#### 4. **AWS (Amazon Web Services)**
- **장점**: 매우 안정적, 확장 가능
- **비용**: 사용량 기반 (월 $5-20)
- **URL**: https://aws.amazon.com

#### 5. **Google Cloud Platform**
- **장점**: 무료 크레딧 제공, 안정적
- **비용**: 무료 크레딧 후 사용량 기반
- **URL**: https://cloud.google.com

#### 6. **DigitalOcean**
- **장점**: 간단한 VPS, 가격 합리적
- **비용**: 월 $5부터
- **URL**: https://www.digitalocean.com

---

## 🚀 방법 1: Railway 배포 (가장 추천)

### 준비사항
- GitHub 계정
- Railway 계정 (GitHub로 가입)

### 배포 단계

#### 1단계: GitHub에 코드 업로드
```bash
# Git 초기화 (아직 안 했다면)
git init
git add .
git commit -m "Initial commit"

# GitHub 저장소 생성 후
git remote add origin https://github.com/yourusername/onnuri-print-shop.git
git push -u origin main
```

#### 2단계: Railway에 배포
1. https://railway.app 접속
2. "Start a New Project" 클릭
3. "Deploy from GitHub repo" 선택
4. GitHub 저장소 선택
5. 자동으로 배포 시작

#### 3단계: 환경 변수 설정
Railway 대시보드에서 환경 변수 추가:
```
DEPLOYMENT_MODE=production
DOMAIN=https://your-app.railway.app
HOST=0.0.0.0
PORT=5000
```

#### 4단계: 도메인 설정 (선택사항)
1. Railway 대시보드 → Settings → Domains
2. Custom Domain 추가: `print7123-1.com`
3. DNS 설정:
   - CNAME: `print7123-1.com` → `your-app.railway.app`

### 장점
- ✅ 자동 배포 (GitHub push 시)
- ✅ 무료 티어 제공
- ✅ 24시간 구동
- ✅ SSL 자동 설정

---

## 🌐 방법 2: Render 배포

### 배포 단계

#### 1단계: GitHub에 코드 업로드
(위와 동일)

#### 2단계: Render에 배포
1. https://render.com 접속
2. "New +" → "Web Service" 선택
3. GitHub 저장소 연결
4. 설정:
   - **Name**: onnuri-print-shop
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app_enhanced_작동중.py`
   - **Plan**: Free

#### 3단계: 환경 변수 설정
Render 대시보드 → Environment:
```
DEPLOYMENT_MODE=production
DOMAIN=https://onnuri-print-shop.onrender.com
HOST=0.0.0.0
PORT=10000
```

### 주의사항
- ⚠️ 무료 플랜은 15분 비활성 시 슬리프 모드 (첫 요청 시 느림)
- ✅ 유료 플랜($7/월)은 24시간 구동

---

## ☁️ 방법 3: PythonAnywhere 배포

### 배포 단계

#### 1단계: PythonAnywhere 가입
1. https://www.pythonanywhere.com 접속
2. 무료 계정 생성

#### 2단계: 파일 업로드
1. Files 탭에서 파일 업로드
2. 또는 GitHub에서 클론:
```bash
git clone https://github.com/yourusername/onnuri-print-shop.git
```

#### 3단계: Web App 설정
1. Web 탭 → "Add a new web app"
2. Python 3.9 선택
3. Manual configuration 선택
4. WSGI 파일 수정:
```python
import sys
path = '/home/yourusername/onnuri-print-shop'
if path not in sys.path:
    sys.path.append(path)

from app_enhanced_작동중 import app as application
```

#### 4단계: 환경 변수 설정
Web 탭 → Environment variables:
```
DEPLOYMENT_MODE=production
DOMAIN=https://yourusername.pythonanywhere.com
```

### 제한사항
- ⚠️ 무료 플랜은 외부 URL 접근 제한
- ✅ 유료 플랜($5/월)은 제한 없음

---

## 🐳 방법 4: Docker 배포 (모든 플랫폼)

### Dockerfile 생성
```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV DEPLOYMENT_MODE=production
ENV DOMAIN=http://localhost
ENV HOST=0.0.0.0
ENV PORT=5000

EXPOSE 5000

CMD ["python", "app_enhanced_작동중.py"]
```

### 사용 방법
```bash
# 이미지 빌드
docker build -t onnuri-print-shop .

# 컨테이너 실행
docker run -d -p 5000:5000 \
  -e DEPLOYMENT_MODE=production \
  -e DOMAIN=https://your-domain.com \
  onnuri-print-shop
```

---

## 🔧 클라우드 배포 전 준비사항

### 1. 환경 변수 설정 파일 생성
`.env` 파일 생성 (로컬 테스트용):
```
DEPLOYMENT_MODE=production
DOMAIN=https://your-app-url.com
HOST=0.0.0.0
PORT=5000
SECRET_KEY=your-secret-key-here
```

### 2. 이메일 설정
클라우드 환경에서는 환경 변수로 설정:
```
MAIL_SERVER=smtp.naver.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=print7123@naver.com
MAIL_PASSWORD=your-email-password
```

### 3. 데이터베이스 백업
로컬 데이터베이스를 클라우드로 복사:
```bash
# SQLite 데이터베이스 백업
cp instance/onnuri_print_enhanced.db backup.db
```

### 4. 포트 설정 확인
클라우드 플랫폼마다 포트가 다를 수 있음:
```python
# app_enhanced_작동중.py에서
PORT = int(os.environ.get('PORT', 5000))  # 환경 변수 우선
```

---

## 📝 클라우드별 포트 설정

### Railway
- 포트: 환경 변수 `PORT` 사용 (자동 할당)

### Render
- 포트: `10000` (무료 플랜) 또는 환경 변수 `PORT`

### PythonAnywhere
- 포트: 자동 관리 (설정 불필요)

### AWS/Azure/GCP
- 포트: `5000` 또는 환경 변수 `PORT`

---

## 🔐 보안 설정

### 1. SECRET_KEY 변경
```python
# 환경 변수로 설정 (절대 코드에 하드코딩하지 말 것)
SECRET_KEY = os.environ.get('SECRET_KEY', 'change-this-in-production')
```

### 2. 데이터베이스 보안
- SQLite 파일 권한 설정
- 정기 백업

### 3. 업로드 파일 제한
```python
MAX_CONTENT_LENGTH = 100 * 1024 * 1024  # 100MB
```

---

## 📊 모니터링

### 로그 확인
- Railway: 대시보드 → Deployments → Logs
- Render: 대시보드 → Logs
- PythonAnywhere: Tasks → Log files

### 성능 모니터링
- CPU 사용량
- 메모리 사용량
- 요청 수
- 응답 시간

---

## 🚨 문제 해결

### 서버가 시작되지 않음
1. 로그 확인
2. 환경 변수 확인
3. 포트 설정 확인
4. Python 버전 확인

### 데이터베이스 오류
1. 데이터베이스 파일 권한 확인
2. 경로 확인 (절대 경로 사용)
3. 백업에서 복원

### 파일 업로드 오류
1. 업로드 폴더 권한 확인
2. 디스크 공간 확인
3. 파일 크기 제한 확인

---

## 💡 권장 사항

### 무료 시작
→ **Railway** (가장 간단하고 안정적)

### 프로덕션 환경
→ **AWS** 또는 **DigitalOcean** (더 안정적)

### 테스트 환경
→ **Render** 무료 플랜

---

## 📞 지원

문제가 발생하면:
1. 클라우드 플랫폼 로그 확인
2. 환경 변수 확인
3. 포트 및 호스트 설정 확인

---

**이제 PC 없이도 24시간 서버가 구동됩니다!** ☁️🚀

