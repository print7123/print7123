# 이메일 설정 가이드 📧

## ✅ 자동 설정 완료

이메일 비밀번호를 별도 파일(`email_config.py`)에서 관리하도록 변경했습니다.

**변경 사항:**
- 이메일 설정이 `email_config.py` 파일로 분리되었습니다
- 비밀번호 변경 시 `email_config.py` 파일만 수정하면 됩니다
- 서버 재시작 없이 설정 파일만 수정하면 자동으로 적용됩니다 (서버 재시작 필요)

---

## 📝 비밀번호 설정 방법

### 1단계: email_config.py 파일 열기

**파일 위치:**
```
현재작동파일_백업_1/현재작동파일_백업_20250913_204500/email_config.py
```

### 2단계: 비밀번호 입력

파일을 열면 다음과 같이 되어 있습니다:

```python
MAIL_PASSWORD = 'your-app-password'  # 여기에 실제 앱 비밀번호 입력
```

**변경:**
```python
MAIL_PASSWORD = '실제_네이버_앱_비밀번호_16자리'  # 여기에 실제 앱 비밀번호 입력
```

### 3단계: 네이버 앱 비밀번호 생성 (아직 안 했다면)

1. **네이버 메일 접속**
   - https://mail.naver.com
   - 로그인

2. **환경설정 이동**
   - 우측 상단 설정 아이콘 클릭
   - "환경설정" 선택

3. **보안 설정**
   - "보안" 탭 선택
   - "2단계 인증" 활성화 (아직 안 했다면)

4. **앱 비밀번호 생성**
   - "보안" → "앱 비밀번호" 선택
   - "앱 비밀번호 생성" 클릭
   - 앱 이름: "온누리인쇄나라" (또는 원하는 이름)
   - 생성된 16자리 비밀번호 복사
   - ⚠️ **중요**: 이 비밀번호는 다시 볼 수 없으므로 반드시 저장!

5. **email_config.py에 입력**
   ```python
   MAIL_PASSWORD = '생성한_앱_비밀번호_16자리'
   ```

### 4단계: 서버 재시작

1. 서버 중지: `Ctrl + C`
2. 서버 재시작: `서버_바로실행.bat`
3. 서버 시작 시 다음 메시지 확인:
   ```
   ✅ 이메일 설정이 올바르게 로드되었습니다.
      발신자: print7123@naver.com
      SMTP 서버: smtp.naver.com:587
   ```

### 5단계: 테스트

1. 관리자 대시보드 접속 (`/admin`)
2. "이메일 전송 테스트" 섹션으로 스크롤
3. 테스트 이메일 주소 입력
4. "테스트 이메일 전송" 버튼 클릭
5. 결과 확인

---

## 🔒 보안 주의사항

### ⚠️ 중요

1. **email_config.py 파일은 절대 공유하지 마세요**
   - 이 파일에는 실제 비밀번호가 들어있습니다
   - GitHub 등에 업로드하지 마세요

2. **.gitignore 추가 (선택사항)**
   - Git을 사용하는 경우 `.gitignore`에 추가:
   ```
   email_config.py
   ```

3. **비밀번호 관리**
   - 비밀번호를 안전한 곳에 별도로 저장하세요
   - 비밀번호를 잊어버리면 네이버에서 재생성해야 합니다

---

## 🔄 비밀번호 변경 방법

### 네이버 앱 비밀번호 변경

1. **기존 앱 비밀번호 삭제**
   - 네이버 메일 → 환경설정 → 보안 → 앱 비밀번호
   - 기존 "온누리인쇄나라" 앱 비밀번호 삭제

2. **새 앱 비밀번호 생성**
   - 새 앱 비밀번호 생성
   - 16자리 비밀번호 복사

3. **email_config.py 수정**
   ```python
   MAIL_PASSWORD = '새로운_앱_비밀번호_16자리'
   ```

4. **서버 재시작**
   - 서버 중지 후 재시작

---

## ✅ 확인 방법

### 서버 시작 시 확인

**정상 설정 시:**
```
✅ 이메일 설정이 올바르게 로드되었습니다.
   발신자: print7123@naver.com
   SMTP 서버: smtp.naver.com:587
```

**비밀번호 미설정 시:**
```
⚠️ 이메일 비밀번호가 설정되지 않았습니다!
================================================================================
📝 설정 방법:
   1. email_config.py 파일을 엽니다
   2. MAIL_PASSWORD = 'your-app-password' 부분을 찾습니다
   3. 'your-app-password'를 실제 네이버 앱 비밀번호로 변경합니다
   4. 네이버 메일 → 환경설정 → 보안 → 2단계 인증 → 앱 비밀번호 생성
================================================================================
```

### 이메일 전송 테스트

1. 관리자 대시보드에서 이메일 테스트
2. 성공 메시지 확인
3. 실제 이메일 수신 확인

---

## 📋 파일 구조

```
현재작동파일_백업_1/
└── 현재작동파일_백업_20250913_204500/
    ├── app_enhanced_작동중.py  (메인 앱 파일)
    └── email_config.py         (이메일 설정 파일) ⭐ 새로 생성됨
```

---

## 🚀 완료

이제 이메일 비밀번호를 `email_config.py` 파일에서 관리할 수 있습니다!

**다음 단계:**
1. `email_config.py` 파일 열기
2. `MAIL_PASSWORD`에 실제 네이버 앱 비밀번호 입력
3. 서버 재시작
4. 이메일 테스트

**변경할 때:**
- `email_config.py` 파일만 수정
- 서버 재시작
- 끝!


