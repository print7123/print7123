# 서버 자동 지속 구동 가이드
## 온누리인쇄나라 서버 자동 실행 설정

**PC가 재시작되어도 서버가 자동으로 시작되도록 설정하는 가이드입니다.**

서버가 자동으로 지속적으로 구동되도록 설정하는 여러 방법을 제공합니다.

---

## 🎯 빠른 시작 (PC 재시작 시 자동 실행)

### ⭐ 가장 간단한 방법: `서버_즉시_시작_및_자동실행.bat`
1. 파일을 더블클릭
2. 서버가 즉시 시작되고 PC 재시작 시 자동 실행 설정 완료
3. 끝!

### ⭐⭐ 가장 확실한 방법: `PC재시작_자동실행_설정.bat`
1. 파일을 우클릭 → "관리자 권한으로 실행"
2. 모든 자동 실행 방법이 자동으로 설정됩니다
3. PC 재시작 후에도 서버가 자동으로 시작됩니다

---

## 📋 방법 선택 가이드

### 방법 1: 자동 재시작 스크립트 (가장 간단) ⭐ 추천
- **장점**: 설정 간단, 즉시 사용 가능
- **단점**: 사용자 로그인 필요
- **파일**: `서버_자동_실행_지속구동.bat`

### 방법 2: Windows 서비스 등록 (가장 안정적) ⭐⭐⭐
- **장점**: Windows 시작 시 자동 실행, 로그인 불필요, 안정적
- **단점**: NSSM 설치 필요
- **파일**: `Windows_서비스_등록.bat`

### 방법 3: 작업 스케줄러 등록
- **장점**: Windows 기본 기능 사용
- **단점**: 관리자 권한 필요
- **파일**: `작업스케줄러_등록.bat`

### 방법 4: 백그라운드 실행
- **장점**: 간단한 백그라운드 실행
- **단점**: 수동 재시작 필요
- **파일**: `서버_백그라운드_실행.bat`

---

## 🚀 방법 1: 자동 재시작 스크립트 (추천)

### 사용 방법
1. `서버_자동_실행_지속구동.bat` 파일을 더블클릭
2. 서버가 종료되면 자동으로 5초 후 재시작됩니다
3. 완전히 종료하려면 창을 닫으세요

### 특징
- ✅ 서버 오류 시 자동 재시작
- ✅ 간단한 설정
- ✅ 로그 확인 가능
- ⚠️ 사용자 로그인 필요

### Windows 시작 시 자동 실행 설정
1. `Win + R` 키를 누르고 `shell:startup` 입력
2. `서버_자동_실행_지속구동.bat`의 바로가기를 생성하여 시작 폴더에 복사

---

## 🔧 방법 2: Windows 서비스 등록 (가장 안정적)

### 준비사항
- 관리자 권한
- 인터넷 연결 (NSSM 자동 다운로드용)

### 사용 방법
1. **관리자 권한으로 실행**
   - `Windows_서비스_등록.bat` 파일을 우클릭
   - "관리자 권한으로 실행" 선택

2. **서비스 등록**
   - 스크립트가 자동으로 NSSM을 다운로드하고 설치합니다
   - 서비스 등록 확인 시 `Y` 입력

3. **서비스 확인**
   - `Win + R` → `services.msc` 입력
   - "온누리인쇄나라 웹서버" 서비스 확인

### 서비스 관리 명령어

```batch
REM 서비스 시작
net start PrintShopService

REM 서비스 중지
net stop PrintShopService

REM 서비스 상태 확인
sc query PrintShopService

REM 서비스 제거
nssm remove PrintShopService confirm
```

### 장점
- ✅ Windows 시작 시 자동 실행
- ✅ 사용자 로그인 불필요
- ✅ 서버 오류 시 자동 재시작 (Windows 서비스 관리)
- ✅ 안정적인 백그라운드 실행

---

## 📅 방법 3: 작업 스케줄러 등록

### 사용 방법
1. **관리자 권한으로 실행**
   - `작업스케줄러_등록.bat` 파일을 우클릭
   - "관리자 권한으로 실행" 선택

2. **작업 등록**
   - 확인 시 `Y` 입력

3. **작업 확인**
   - `Win + R` → `taskschd.msc` 입력
   - "온누리인쇄나라_서버_자동실행" 작업 확인

### 작업 관리 명령어

```batch
REM 작업 실행
schtasks /run /tn "온누리인쇄나라_서버_자동실행"

REM 작업 중지
schtasks /end /tn "온누리인쇄나라_서버_자동실행"

REM 작업 삭제
schtasks /delete /tn "온누리인쇄나라_서버_자동실행" /f

REM 작업 확인
schtasks /query /tn "온누리인쇄나라_서버_자동실행"
```

---

## 💻 방법 4: 백그라운드 실행

### 사용 방법
1. `서버_백그라운드_실행.bat` 파일을 더블클릭
2. 서버가 최소화된 창에서 실행됩니다
3. 작업 표시줄에서 "온누리인쇄나라 서버" 창 확인

### 특징
- ✅ 백그라운드 실행
- ✅ 간단한 사용
- ⚠️ 수동 재시작 필요

---

## 🔍 서버 상태 확인

### 포트 확인
```batch
netstat -ano | findstr :5000
```

### 프로세스 확인
```batch
tasklist | findstr python
```

### 서버 로그 확인
- 방법 1, 4: 실행 창에서 직접 확인
- 방법 2, 3: Windows 이벤트 뷰어에서 확인

---

## 🛠️ 문제 해결

### 서버가 시작되지 않음
1. Python 설치 확인: `python --version`
2. 필요한 패키지 설치: `pip install -r requirements.txt`
3. 포트 충돌 확인: `netstat -ano | findstr :5000`

### 서비스가 시작되지 않음
1. 관리자 권한으로 실행했는지 확인
2. 이벤트 뷰어에서 오류 확인
3. Python 경로 확인

### 작업 스케줄러가 실행되지 않음
1. 관리자 권한으로 실행했는지 확인
2. 작업 스케줄러 서비스 실행 확인
3. 작업 설정 확인

---

## 📝 권장 설정

### 일반 사용자
→ **방법 1: 자동 재시작 스크립트** + Windows 시작 폴더에 추가

### 서버 관리자
→ **방법 2: Windows 서비스 등록** (가장 안정적)

### 테스트 환경
→ **방법 4: 백그라운드 실행**

---

## ⚙️ 고급 설정

### 서버 재시작 간격 조정
`서버_자동_실행_지속구동.bat` 파일에서 다음 줄 수정:
```batch
timeout /t 5 /nobreak >nul  REM 5초를 원하는 시간으로 변경
```

### 서비스 자동 재시작 설정
서비스 등록 후:
1. `services.msc` 실행
2. "온누리인쇄나라 웹서버" 서비스 선택
3. 속성 → 복구 탭
4. 실패 시 작업을 "서비스 다시 시작"으로 설정

---

## 📞 지원

문제가 발생하면:
1. 서버 로그 확인
2. Windows 이벤트 뷰어 확인
3. 포트 및 프로세스 상태 확인

---

**서버가 이제 자동으로 지속적으로 구동됩니다!** 🎉

