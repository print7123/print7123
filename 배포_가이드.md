# 온누리인쇄나라 홈페이지 배포 가이드
## 도메인: http://print7123-1.com

### 📋 목차
1. [배포 전 준비사항](#배포-전-준비사항)
2. [배포 방법](#배포-방법)
3. [서버 설정](#서버-설정)
4. [도메인 설정](#도메인-설정)
5. [보안 설정](#보안-설정)
6. [문제 해결](#문제-해결)

---

## 배포 전 준비사항

### 1. 필수 요구사항
- Python 3.7 이상
- 필요한 Python 패키지 (requirements.txt)
- 서버 접근 권한
- 도메인 DNS 설정 완료

### 2. 파일 확인
다음 파일들이 있는지 확인하세요:
- `app_enhanced_작동중.py` - 메인 Flask 애플리케이션
- `deploy_config.py` - 배포 설정 파일
- `wsgi.py` - WSGI 진입점
- `requirements.txt` - 필요한 패키지 목록
- `배포_실행.bat` - Windows 배포 스크립트

---

## 배포 방법

### 방법 1: 기본 Flask 서버 (개발/테스트용)

#### Windows
```batch
배포_실행.bat
```

또는 수동 실행:
```batch
set DEPLOYMENT_MODE=production
set DOMAIN=http://print7123-1.com
python app_enhanced_작동중.py
```

#### Linux/Mac
```bash
export DEPLOYMENT_MODE=production
export DOMAIN=http://print7123-1.com
python3 app_enhanced_작동중.py
```

### 방법 2: WSGI 서버 사용 (프로덕션 권장)

#### Gunicorn (Linux/Mac)
```bash
# 설치
pip install gunicorn

# 실행
gunicorn -w 4 -b 0.0.0.0:5000 wsgi:application
```

#### Waitress (Windows/Linux/Mac)
```bash
# 설치
pip install waitress

# 실행
waitress-serve --host=0.0.0.0 --port=5000 wsgi:application
```

#### uWSGI (Linux)
```bash
# 설치
pip install uwsgi

# 실행
uwsgi --http :5000 --module wsgi:application --processes 4
```

---

## 서버 설정

### 1. 환경 변수 설정

#### Windows
```batch
set DEPLOYMENT_MODE=production
set DOMAIN=http://print7123-1.com
set HOST=0.0.0.0
set PORT=5000
set SECRET_KEY=your-secret-key-here
```

#### Linux/Mac
```bash
export DEPLOYMENT_MODE=production
export DOMAIN=http://print7123-1.com
export HOST=0.0.0.0
export PORT=5000
export SECRET_KEY=your-secret-key-here
```

### 2. 이메일 설정

`email_config.py` 파일을 생성하거나 환경 변수로 설정:

```python
MAIL_SERVER = 'smtp.naver.com'
MAIL_PORT = 587
MAIL_USE_TLS = True
MAIL_USERNAME = 'print7123@naver.com'
MAIL_PASSWORD = 'your-email-password'
```

---

## 도메인 설정

### 1. DNS 설정
도메인 `print7123-1.com`의 DNS 레코드를 서버 IP로 설정:

```
A 레코드:
print7123-1.com → 서버 IP 주소
www.print7123-1.com → 서버 IP 주소 (선택사항)
```

### 2. 리버스 프록시 설정 (Nginx/Apache)

#### Nginx 설정 예시
```nginx
server {
    listen 80;
    server_name print7123-1.com www.print7123-1.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 정적 파일 직접 서빙
    location /static {
        alias /path/to/your/static;
        expires 30d;
    }

    # 업로드 파일 서빙
    location /uploads {
        alias /path/to/your/uploads;
    }
}
```

#### Apache 설정 예시
```apache
<VirtualHost *:80>
    ServerName print7123-1.com
    ServerAlias www.print7123-1.com

    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:5000/
    ProxyPassReverse / http://127.0.0.1:5000/

    # 정적 파일 직접 서빙
    Alias /static /path/to/your/static
    <Directory /path/to/your/static>
        Require all granted
    </Directory>
</VirtualHost>
```

### 3. HTTPS 설정 (권장)

SSL 인증서를 설치하고 HTTPS로 리다이렉트:

```nginx
server {
    listen 80;
    server_name print7123-1.com www.print7123-1.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name print7123-1.com www.print7123-1.com;

    ssl_certificate /path/to/certificate.crt;
    ssl_certificate_key /path/to/private.key;

    # ... 나머지 설정
}
```

---

## 보안 설정

### 1. SECRET_KEY 변경
프로덕션 환경에서는 반드시 `SECRET_KEY`를 변경하세요:

```python
# deploy_config.py 또는 환경 변수
SECRET_KEY = '강력한-랜덤-문자열-생성-필요'
```

### 2. 방화벽 설정
필요한 포트만 열어두세요:
- HTTP: 80
- HTTPS: 443
- Flask 앱: 5000 (내부만, 외부 접근 차단)

### 3. 데이터베이스 백업
정기적으로 데이터베이스를 백업하세요:

```bash
# SQLite 백업
cp instance/onnuri_print_enhanced.db backups/backup_$(date +%Y%m%d).db
```

---

## 문제 해결

### 1. 포트가 이미 사용 중
```bash
# Windows
netstat -ano | findstr :5000
taskkill /F /PID [PID번호]

# Linux
lsof -i :5000
kill -9 [PID번호]
```

### 2. 권한 오류
```bash
# 파일 권한 확인 및 수정
chmod 755 app_enhanced_작동중.py
chmod -R 755 static/
chmod -R 755 uploads/
```

### 3. 모듈을 찾을 수 없음
```bash
# 필요한 패키지 재설치
pip install -r requirements.txt
```

### 4. 데이터베이스 오류
```bash
# 데이터베이스 파일 권한 확인
chmod 644 instance/onnuri_print_enhanced.db
```

---

## 자동 시작 설정 (선택사항)

### Windows 서비스로 등록
NSSM (Non-Sucking Service Manager) 사용:
```batch
nssm install PrintShopService "C:\Python\python.exe" "C:\path\to\app_enhanced_작동중.py"
nssm set PrintShopService AppEnvironmentExtra DEPLOYMENT_MODE=production DOMAIN=http://print7123-1.com
nssm start PrintShopService
```

### Linux systemd 서비스
`/etc/systemd/system/printshop.service`:
```ini
[Unit]
Description=온누리인쇄나라 웹사이트
After=network.target

[Service]
User=www-data
WorkingDirectory=/path/to/your/app
Environment="DEPLOYMENT_MODE=production"
Environment="DOMAIN=http://print7123-1.com"
ExecStart=/usr/bin/python3 /path/to/your/app/app_enhanced_작동중.py
Restart=always

[Install]
WantedBy=multi-user.target
```

서비스 시작:
```bash
sudo systemctl enable printshop
sudo systemctl start printshop
```

---

## 모니터링

### 로그 확인
```bash
# 애플리케이션 로그
tail -f logs/app.log

# 시스템 로그 (Linux)
journalctl -u printshop -f
```

### 성능 모니터링
- 서버 리소스 사용량 확인
- 데이터베이스 크기 모니터링
- 업로드 폴더 용량 확인

---

## 지원

문제가 발생하면 다음을 확인하세요:
1. 로그 파일 확인
2. 서버 상태 확인
3. 네트워크 연결 확인
4. DNS 설정 확인

---

**배포 완료 후 `http://print7123-1.com`에서 접속하여 정상 작동을 확인하세요!**

